# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
.next/
out/
dist/
build/

# Environment files — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
logs/
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Vercel
.vercel

.env.txt
*.env.txt

# Python (ops/ harness build artifacts)
__pycache__/
*.pyc

# npm workspace lockfile — the repo deliberately carries none (CLAUDE.md: CI runs
# `npm install`, not `npm ci`), and an `npm i` at the root generates one.
/package-lock.json
